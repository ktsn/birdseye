version: 2

defaults: &defaults
  working_directory: ~/working/repo
  docker:
    - image: circleci/node:8.11.3

jobs:
  install:
    <<: *defaults
    steps:
      - checkout

      # Download and cache dependencies
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "yarn.lock" }}
            # fallback to using the latest cache if no exact match is found
            - v1-dependencies-

      - run: npx lerna bootstrap

      - save_cache:
          paths:
            - node_modules
            - packages/@birdseye/core/node_modules
            - packages/@birdseye/app/node_modules
            - packages/@birdseye/vue/node_modules
          key: v1-dependencies-{{ checksum "yarn.lock" }}

      - persist_to_workspace:
          root: ~/working
          paths:
            - repo

  build:
    <<: *defaults
    steps:
      - attach_workspace:
          at: ~/working
      - run: npx lerna run build

  test:
    <<: *defaults
    steps:
      - attach_workspace:
          at: ~/working
      - run: npx lerna run test

workflows:
  version: 2
  build_and_test:
    jobs:
      - install
      - build:
          requires:
            - install
      - test:
          requires:
            - install