{
  "name": "birdseye",
  "private": true,
  "workspaces": [
    "packages/@birdseye/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap && yarn build",
    "test": "lerna run test",
    "build": "lerna run build",
    "catalog": "lerna run catalog"
  },
  "devDependencies": {
    "lerna": "^3.2.1"
  }
}
